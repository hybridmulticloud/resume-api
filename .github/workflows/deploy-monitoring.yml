name: Deploy Monitoring

on:
  push:
    paths:
      - 'monitoring/**'
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

env:
  TF_VAR_alert_email:         ${{ secrets.ALERT_EMAIL_ADDRESS }}
  TF_VAR_backend_state_bucket: ${{ secrets.BACKEND_STATE_BUCKET }}
  TF_VAR_backend_state_key:    ${{ secrets.BACKEND_STATE_KEY }}
  TF_VAR_backend_lock_table:   ${{ secrets.BACKEND_LOCK_TABLE }}
  TF_VAR_aws_region:           us-east-1

jobs:
  monitoring:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-region:            ${{ env.TF_VAR_aws_region }}
          aws-access-key-id:     ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Clean Terraform cache
        run: |
          cd monitoring
          rm -rf .terraform .terraform.lock.hcl

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: "1.5.7"

      - name: Terraform Init
        working-directory: monitoring
        run: terraform init -upgrade

      - name: Terraform Validate
        working-directory: monitoring
        run: terraform validate

      - name: Terraform Apply
        working-directory: monitoring
        run: terraform apply -auto-approve -input=false
